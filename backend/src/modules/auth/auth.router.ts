import { Router } from "express";import jwt from "jsonwebtoken";import { env } from "../../config/env.js";import { authenticate,authorize,AuthRequest,Role } from "../../core/auth.js";import { UserService } from "../users/user.service.js";const users=new UserService();export const authRouter=Router();authRouter.post("/login",async(req,res,next)=>{try{const user=await users.login(req.body.email||"",req.body.password||"");if(!user)return res.status(401).json({error:"Invalid credentials"});const token=jwt.sign({id:user._id,email:user.email,role:user.role},env.jwtSecret,{expiresIn:"8h"});res.json({token,user:{id:user._id,email:user.email,role:user.role}})}catch(e){next(e)}});authRouter.get("/me",authenticate,(req,res)=>res.json((req as AuthRequest).user));authRouter.get("/users",authenticate,authorize("superadmin","admin"),async(_req,res,next)=>{try{res.json(await users.list())}catch(e){next(e)}});authRouter.post("/users",authenticate,authorize("superadmin","admin"),async(req,res,next)=>{try{res.status(201).json(await users.create(req.body.email,req.body.password,req.body.role as Role))}catch(e){next(e)}});
