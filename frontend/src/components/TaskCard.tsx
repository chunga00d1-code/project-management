import type { Task } from "../types";
export function TaskCard({ task, onStatus, onOpen, onDelete }: { task: Task; onStatus: (status: string) => void; onOpen: () => void; onDelete: () => void }) {
  const checklist = task.checklist || []; const completed = checklist.filter((item) => item.done).length; const overdue = Boolean(task.dueDate && task.dueDate < new Date().toISOString().slice(0, 10) && !["done", "cancelled"].includes(task.status));
  return <article className={`task-card priority-${task.priority}`} draggable onDragStart={(e) => e.dataTransfer.setData("taskId", task._id)}><div className="task-card-top">{task.code && <span className="badge-code">{task.code}</span>}<span className={`badge-priority ${task.priority}`}>{task.priority}</span>{task.dueDate && <span className={`due-badge ${overdue ? "overdue" : ""}`}>{overdue ? "Quá hạn · " : "Hạn · "}{new Date(`${task.dueDate}T00:00:00`).toLocaleDateString("vi-VN")}</span>}</div>{task.prSyncStatus === "mismatched" && <div className="mismatch-badge" title={(task.prMismatchReasons || []).join(", ")}>⚠️ PR không khớp: {(task.prMismatchReasons || []).join(", ")}</div>}<button className="task-title" onClick={onOpen}>{task.title}</button><div className="task-meta"><span>{task.assignee || "Chưa giao"}</span>{task.project && <span>{task.project}</span>}{task.sprint && <span>{task.sprint}</span>}</div>{task.labels.length > 0 && <div className="label-row">{task.labels.slice(0, 3).map((label) => <span key={label}>#{label}</span>)}{task.labels.length > 3 && <span>+{task.labels.length - 3}</span>}</div>}{checklist.length > 0 && <div className="checklist-progress"><div><span>Checklist</span><strong>{completed}/{checklist.length}</strong></div><progress max={checklist.length} value={completed} /></div>}<label className="status-control"><span>Trạng thái</span><select value={task.status} onChange={(e) => onStatus(e.target.value)}>{["todo", "in_review", "needs_changes", "ready", "done", "cancelled"].map((status) => <option key={status} value={status}>{status.replaceAll("_", " ")}</option>)}</select></label><div className="card-actions"><button className="btn-details" onClick={onOpen}>Xem chi tiết</button><button className="btn-icon-danger" aria-label={`Xóa ${task.title}`} title="Xóa nhiệm vụ" onClick={onDelete}>×</button></div></article>;
}
