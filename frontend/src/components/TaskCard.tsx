import type { Task } from "../types";import { TaskMeta } from "./TaskMeta";export function TaskCard({task,onStatus,onOpen,onDelete}:{task:Task;onStatus:(status:string)=>void;onOpen:()=>void;onDelete:()=>void}){return <article draggable onDragStart={e=>e.dataTransfer.setData("taskId",task._id)}><b>{task.title}</b><p>{task.assignee} � {task.priority}</p><select value={task.status} onChange={e=>onStatus(e.target.value)}>{["todo","in_review","needs_changes","ready","done","cancelled"].map(s=><option key={s}>{s}</option>)}</select><button onClick={onOpen}>Details</button><button onClick={onDelete}>Delete</button></article>}
