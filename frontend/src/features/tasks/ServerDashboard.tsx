import { useEffect, useState } from "react";
import { api } from "../../api/client";
type Dashboard = { total: number; active: number; needsChanges: number; overdue: number; blocked: number; byRepository: { repository: string; count: number }[]; byProject: { project: string; count: number }[]; byAssignee: { assignee: string; count: number }[]; };
export function ServerDashboard() { const [value, setValue] = useState<Dashboard>(); useEffect(() => { void api<Dashboard>("/tasks/dashboard").then(setValue); }, []); if (!value) return null; return <section><strong>Total: {value.total}</strong><strong>Active: {value.active}</strong><strong>Needs changes: {value.needsChanges}</strong><strong>Overdue: {value.overdue}</strong><strong>Blocked: {value.blocked}</strong><ul>{value.byRepository.map((item) => <li key={item.repository}>Repository · {item.repository}: {item.count}</li>)}{value.byProject.map((item) => <li key={item.project}>Project · {item.project}: {item.count}</li>)}{value.byAssignee.map((item) => <li key={item.assignee}>Active workload · {item.assignee}: {item.count}</li>)}</ul></section>; }

